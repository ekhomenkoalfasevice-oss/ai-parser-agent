## AstroForecast Bot — Product & Technical Specification

### 1. Purpose & Outcomes
- **Mission**: предоставить пользователям персональный астропрогноз с высокой скоростью (≤5 с) и конвертировать их в подписку, которая раскрывает детальные расклады, архив и сервис «Экстренный вопрос».
- **Primary KPI**: 25 % конверсия из бесплатного прогноза в просмотр апселла, 8–10 % переход в платёж, удержание 30‑дневной подписки ≥60 %.
- **Scope**: Telegram-бот + Telegram-канал «Главное астрособытие дня» на едином бэкенде (FastAPI + Celery). Расширение в web/embed рассматривается как Iteration 3.

### 2. Personas & Core Jobs
- **Astro Explorer (free)**: 20–35 лет, проверяет ежедневные подсказки, чувствителен к «тональности дня». Job: быстро понять настроение и ключевой акцент.
- **Intentional Planner (paid)**: 25–40 лет, строит планы по финансам/отношениям, нуждается в деталях и архиве. Job: сопоставить события с транзитами, получить actionable советы.
- **Seeker in Crisis (paid)**: ищет быстрый совет «что делать сейчас». Job: задать экстренный вопрос и получить поддержку 1 раз в день.

### 3. Value Ladder & Monetization
1. **Free short forecast** → мгновенный инсайт + CTA.
2. **Trial 48 h** → полный доступ к раскладам, архиву, «Экстренному вопросу».
3. **Subscription**: Weekly (99 ₽) / Monthly (150 ₽) / Upgrade to Quarterly (390 ₽) в Iteration 2.
4. **Retention mechanics**: архив с пометкой «сбылось», рассылки, пуши «Задай экстренный вопрос».

### 4. Feature Breakdown & Detailed Flows
#### 4.1 Onboarding & Profile Capture
- `/start` → приветствие, запрос даты рождения (валидация 1900–2020).
- После даты: запрос времени и города (формат `чч:мм, Город`). Если пропуск → дефолт 12:00 и timezone по IP.
- Геокодер (Nominatim/Photon) получает lat/lon + tz.
- Настраиваем SLA: ≤2 round-trips (дата → время/город → подтверждение).

#### 4.2 Short Forecast (Free Tier)
- Input: `birth_profile`, `current_transits`, параметры шаблона (`Скорость/Глубина/Осторожность`).
- Decision logic:
  - `Скорость` → высокий уровень активных транзитов (Марс, Луна).
  - `Глубина` → акценты на Плутон/Нептун/Скорпионские транзиты.
  - `Осторожность` → присутствие квадратов/оппозиций, ретроградные планеты.
- Output: 2–3 предложения, ≤380 символов, includes emoji + CTA button «Получить полный расклад».
- Cache strategy: Redis keyed by `user_id + date`. Повторный запрос → тот же текст.

#### 4.3 Upsell & Payments
- После короткого прогноза показать карусель преимуществ (Inline keyboard + Media message).
- Кнопки тарифов: `Триал 2 дня`, `Неделя 99₽`, `Месяц 150₽`.
- Telegram Payments (Stars) default, fallback Stripe checkout link.
- Payment flow:
  1. Bot → `/subscriptions/checkout`.
  2. Получаем invoice payload → отправляем инвойс.
  3. Webhook `/payments/webhook` подтверждает статус.
  4. Trial создаёт подписку со статусом `active`, `expires_at = now + 48h`.

#### 4.4 Full Forecast (Subscribers)
- Доступен 1 раз в календарный день (вторая выдача → показать сохранённый).
- Структура секций:
  1. `Финансы` — акцент на Марс/Юпитер/Венера в 2/8/10 домах.
  2. `Любовь/отношения` — Венера/Луна/7 дом, эмоциональный тон.
  3. `Здоровье` — ритмы Луны, ретроградности, совет по телу.
  4. `Рекомендация дня` — формат: `ритуал / аффирмация / действие`.
- Output формат: Markdown с эмодзи, bullet-ы для читабельности.
- Сохранение в `forecasts` с `type=full`, `transit_snapshot`.

#### 4.5 Archive & Feedback
- `/archive` → последние 7 дат кнопками, кнопка `Показать ещё`.
- Просмотр → текст + кнопки `Сбылось ✅ / Не сбылось ✖`.
- Feedback сохраняется для personalization (LLM prompt получает тег `accuracy=high/low`).

#### 4.6 Emergency Question («Экстренный вопрос»)
- Доступ только у активной подписки, лимит 1/24h → Redis TTL.
- Mini-chat режим:
  1. Prompt: безопасный стиль, максимум 120 слов, обязательный дисклеймер.
  2. Ответ можно поделить на 2–3 сообщения (проблема → совет → next step).
  3. Кнопка `Задать ещё завтра` выводит таймер.

#### 4.7 Daily Broadcast «Главное астрособытие дня»
- Ночной Celery-beat job вычисляет ключевой транзит (по весам: аспект * близость * вовлечённые планеты).
- LLM генерирует текст 250–300 символов + совет.
- Публикация:
  - Telegram канал (bot API `sendMessage`).
  - Push подписчикам (опция в настройках).

#### 4.8 Notifications & Retention
- Типы:
  1. `daily_reminder` — если нет открытия короткого прогноза до 12:00 локального времени.
  2. `trial_expiring` — за 12 ч до окончания триала.
  3. `subscription_expiring` — за 1 день до конца.
  4. `sos_invite` — если функция «Экстренный вопрос» не использовалась 3 дня.
- Канал: Telegram push (bot sendMessage) + опционально email (Iter 3).

#### 4.9 Admin & Analytics
- Web dashboard (FastAPI + HTMX или separate Next.js) с авторизацией через Telegram Login.
- KPI cards: активные подписки, MRR, конверсия, usage «Экстренный вопрос».
- Manual broadcast composer.

### 5. Technical Architecture
| Layer | Responsibility | Tech Stack |
| --- | --- | --- |
| Bot Gateway | Webhook, FSM, keyboards, localization | FastAPI + aiogram v3, Redis FSM storage |
| Core API | Users, forecasts, subscriptions | FastAPI, SQLAlchemy, Pydantic |
| Astrology Service | Натал + транзиты, caching eph data | pyswisseph, Swiss Ephemeris files |
| LLM Orchestrator | Prompt templates, guardrails, caching | LangChain / custom, OpenAI GPT‑4.1 or GPT‑4o-mini |
| Forecast Engine | Celery tasks, scheduling, daily broadcast | Celery workers + Redis broker |
| Storage | Primary data & caching | PostgreSQL 15, Redis 7, MinIO/S3 for logs |
| Payments | Stars/Stripe/YooKassa integration | Provider SDKs |
| Observability | Logs, metrics, tracing | Prometheus, Grafana, Sentry |

Deployment: Docker images per service, docker-compose for dev, IaC (Terraform) for prod on Hetzner/Render. CI/CD via GitHub Actions (lint → tests → staging → manual prod).

### 6. Data Model Deep Dive
- `users`: birth data, geo, timezone, notification toggles, consent flags.
- `natal_charts`: heavy JSON blobs (planet positions, houses, aspects) cached to avoid recomputation.
- `forecasts`: 
  - `content_short` (TEXT) + `content_full` (JSONB).
  - `metrics`: `speed_tag`, `depth_tag`, `caution_tag`.
- `subscriptions`: includes `provider`, `external_id`, `auto_renew`.
- `emergency_questions`: `question`, `answer`, `status` (pending/sent/error), `next_allowed_at`.
- `events_log`: append-only for analytics (user_id, event, payload JSONB).

### 7. LLM Prompt Strategy & Guardrails
- **Short forecast template**:
  - Inputs: `sun_sign`, `moon_phase`, top 3 transits, `speed/depth/caution` levels, `user_name?`.
  - Output: 2–3 предложения, upbeat but balanced, includes emoji, CTA referencing полный расклад.
- **Full forecast template**:
  - Structured JSON: `{"finance": {...}, "love": {...}, "health": {...}, "advice": {...}}`.
  - Each section ≤90 слов, содержит конкретное действие.
  - Post-processing в Markdown, добавляются эмодзи и подчёркивания.
- **Emergency prompt**:
  - Style: caring advisor, максимум 120 слов, содержит дисклеймер «Это не финансовая/медицинская консультация».
  - Safety filters: ключевые слова (самоповреждение, медицина) → handoff скрипту (сообщение о поддержке).
- **Fallback**: rule-based текст, если LLM недоступен (predefined шаблоны на сочетания знаков).

### 8. Conversation Prototypes (Expanded)
```startLine:endLine:docs/astro_bot_detailed_spec.md
// ... existing code ...
```

| Flow | Steps | Sample utterances |
| --- | --- | --- |
| **1. Onboarding** | `/start` → дата → время/город → подтверждение | Бот: «Привет! ... Введи дату рождения (дд.мм.гггг).» → Пользователь: «12.07.1993» → Бот: «Есть ли время и город? Напиши `чч:мм, город` или нажми “Пропустить”.» → Пользователь: «10:45, Москва» → Бот: «Спасибо! Считаю твой космос…» |
| **2. Бесплатный прогноз** | расчёт → вывод + CTA | Бот: «Сегодня Луна соединяется с твоим Солнцем — всплеск энергии. Действуй быстро, но проверь детали. Хочешь полный расклад про финансы, любовь и здоровье? 2 дня бесплатно.» Кнопки: `Получить полный расклад`, `Позже`. |
| **3. Подписка** | выбор тарифа → инвойс → успех → фулл | Бот: «Подписка “Глубокий космос”: • Детальные расклады • Архив • Экстренный вопрос. Выбери тариф.» После оплаты: «✅ Подписка активна до 20.11. Вот твой полный расклад.» |
| **4. Полный расклад** | выдача секций | Бот: «Финансы — Марс активирует денежный сектор, договорись до обеда. Любовь — Венера и Луна в гармонии, говори о чувствах. Здоровье — помоги телу водой. Рекомендация — аффирмация “Я выбираю лёгкость и точность”.» Кнопки: `Архив`, `Экстренный вопрос`, `Настройки`. |
| **5. Архив** | выбор даты → показ → feedback | Пользователь: «Архив». Бот: «Выбери дату (последние 7).» → После выбора: «Прогноз от 12.11: … Было ли актуально?» Кнопки: `Сбылось ✅`, `Не сбылось ✖`. |
| **6. Экстренный вопрос** | проверка лимита → вопрос → ответ | Бот: «Экстренный вопрос доступен 1 раз в сутки. Что волнует?» → Пользователь: «Стоит ли брать новый проект?» → Бот: «Меркурий активен, но квадрат к Сатурну просит деталей. Возьми паузу, собери факты, затем решай. (Повтори вопрос завтра, если нужно).» |
| **7. Напоминание** | триггер push | Бот: «Ты ещё не открыл прогноз на сегодня. Луна в Деве поддержит планирование — загляни, чтобы ничего не упустить 💫.» |
| **8. Окончание подписки** | предупреждение → CTA | Бот: «До конца подписки осталось 1 день. Продли, чтобы не потерять архив и ежедневные подсказки.» Кнопки: `Продлить на месяц`, `Продлить на неделю`. |

### 9. User Stories & Acceptance Criteria
- **US-001**: как пользователь, я хочу быстро ввести дату рождения, чтобы получить прогноз. *AC*: валидация, ошибки на неверный формат, время ответа ≤5 с.
- **US-010**: как подписчик, я хочу детальный расклад один раз в день. *AC*: недоступно без подписки, повтор выдаёт сохранённый текст.
- **US-020**: как подписчик, я хочу задать экстренный вопрос. *AC*: лимит 1/24h, если лимит исчерпан — показать время следующей доступности.
- **US-030**: как пользователь, я хочу отмечать «сбылось», чтобы получать более точные советы. *AC*: выбор кнопки сохраняет feedback, подтверждение на экране.
- **US-040**: как админ, я хочу видеть активные подписки. *AC*: панель показывает count и MRR, обновление ≤15 мин.

### 10. Roadmap & Work Breakdown
1. **Iteration 0 (Prep, 3–4 дня)**
   - Настроить репозиторий, Docker, базовые модели пользователей, миграции.
   - Интегрировать Telegram webhook + health checks.
2. **Iteration 1 (MVP, 2–3 недели)**
   - Профиль, короткий прогноз, Swiss Ephemeris, LLM prompts, апселл, платежи, полный расклад, архив, базовые уведомления.
3. **Iteration 2 (Engagement, 2 недели)**
   - Экстренный вопрос, оценка «сбылось», расширенные пуши, Admin dashboard v1.
4. **Iteration 3 (Growth, 2 недели)**
   - Веб-виджет, многоязычность, рекомендации и email-канал.

### 11. Testing & Observability Plan
- **Unit**: транзиты (астросервис), формататоры текстов, уведомления.
- **Contract**: API endpoints (users/forecasts/subscriptions) с JSON schemas.
- **LLM QA**: golden prompts + snapshot тесты на токсичность и длину.
- **Load**: рассылка 100k пользователей ≤5 мин, Celery autoscaling.
- **Monitoring**: Prometheus (latency, queue depth), Grafana dashboards, Sentry alerts, log enrichment (request id, user id, prompt id).

### 12. Open Questions / Next Steps
- Уточнить юридические требования для платёжных провайдеров (KYC?).
- Нужно ли хранить IP/Device для анализа? (если да — GDPR notice).
- Решить, будет ли «скорость/глубина/осторожность» настраиваться пользователем или автоматически по транзитам.
- Подготовить контент-голос для канала (tone of voice guide).

Документ служит рабочим ТЗ и отправной точкой для проектирования бота. Следующий артефакт — схемы БД/sequence диаграммы и конкретные промпты.
